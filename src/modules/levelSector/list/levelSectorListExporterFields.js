import model from 'modules/levelSector/levelSectorModel';

const { fields } = model;

export default [
    fields.id,
    fields.created_at,
    fields.updated_at,
    fields.createdAtRange,
];