import model from 'modules/educDirectorCycle/educDirectorCycleModel';

const { fields } = model;

export default [
    fields.id,
    fields.created_at,
    fields.updated_at,
    fields.createdAtRange,
];