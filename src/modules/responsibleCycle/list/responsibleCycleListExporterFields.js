import model from 'modules/responsibleCycle/responsibleCycleModel';

const { fields } = model;

export default [
    fields.id,
    fields.created_at,
    fields.updated_at,
    fields.createdAtRange,
];