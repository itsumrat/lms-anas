import model from 'modules/framerMatterCycle/framerMatterCycleModel';

const { fields } = model;

export default [
    fields.id,
    fields.createdAt,
    fields.updatedAt,
    fields.createdAtRange,
];