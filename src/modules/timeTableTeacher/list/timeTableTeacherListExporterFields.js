import model from "modules/timeTableTeacher/timeTableTeacherModel";

const { fields } = model;

export default [
  fields.id,
  fields.createdAt,
  fields.updatedAt,
  fields.createdAtRange,
];
