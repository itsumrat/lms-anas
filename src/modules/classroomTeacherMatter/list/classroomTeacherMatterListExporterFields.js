import model from 'modules/classroomTeacherMatter/classroomTeacherMatterModel';

const { fields } = model;

export default [
    fields.id,
    fields.created_at,
    fields.updated_at,
    fields.createdAtRange,
];